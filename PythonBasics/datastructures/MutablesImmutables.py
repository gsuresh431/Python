'''
Immutable objects are the objects whose value CANT be changed after it is created
Mutable   objects are the objects whose value CAN  be changed after it is created

Immutable
    - String
Mutable
    -  


'''

# Strings are immutables
a = 'Ranjan'
# a[0] = 'B' # TypeError: 'str' object does not support item assignment